name: dividend

profile: dividend

model-paths: ["models"]
test-paths: ["tests"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dividend:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
